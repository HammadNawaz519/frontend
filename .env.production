# Production environment — Vite reads this at build time on Vercel.
# Set OPENROUTER_API_KEY in your Vercel Project Settings > Environment Variables.
# Do NOT put real keys here — Vercel injects them from the dashboard at build time.
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
